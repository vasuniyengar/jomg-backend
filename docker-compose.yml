services:
  postgres:
    image: postgres:16-alpine
    container_name: jomg-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-jomg}
      POSTGRES_USER: ${POSTGRES_USER:-jomg}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-jomg_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-jomg} -d ${POSTGRES_DB:-jomg}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
